# -*- encoding: utf-8 -*-
# Forza una priorità alta, in modo da installare sip/pyqt
# prima di qualunque altra cosa. SIP può essere necessario per buildare
# librerie, e pyuic per buildare UI.
priority = 10
