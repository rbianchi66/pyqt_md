%ModuleHeaderCode
double area(const Path2d& p);
double sarea(const Path2d& p);
double area(const std::vector<Path2d>& path_list, int num_checks);
%End
double area(const Path2d& p);
double sarea(const Path2d& p);
double area(const std::vector<Path2d>& path_list, int num_checks=1);

